use System::Console;
use System::IO::Path;
use System::Text::StringBuilder;

Console::WriteLineString("typed .NET interop from RustScript");
let name = Path::GetFileName("examples/demo.rss");
Console::WriteLineString(name);

let builder = StringBuilder::NewStringBuilder();
StringBuilder::AppendStringInstance(builder, "CLR ");
StringBuilder::AppendStringInstance(builder, "object handle");
Console::WriteLineString(StringBuilder::ToString(builder));
StringBuilder::ReleaseStringBuilder(builder);
